local widgets = {}

widgets.Button = require("ui.button")
widgets.Slider = require("ui.slider")
widgets.Collapse = require("ui.collapse")
widgets.CollapseDevice = require("ui.collapse_device")
widgets.Selector = require("ui.selector")
widgets.Toggle = require("ui.toggle")
widgets.ToggleSmall = require("ui.toggle_small")
widgets.Dropdown = require("ui.dropdown")
widgets.Meter = require("ui.meter")

return widgets
